/*chrome.tabs.query({'active': true, 'windowId': chrome.windows.WINDOW_ID_CURRENT},
   function(tabs){
    
    let curr = (tabs[0].url);
    let last = curr;

    if (currUrl = 'https://cobalt.tools'){
        document.getElementById("url-input-area").innerHTML = last;
        }
    }
)
*/
//input.value set to url
//document.getElementById('test').setAttribute('id', 'nextstep')